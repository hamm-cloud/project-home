[build]
builder = "nixpacks"

[deploy]
restartPolicyMaxRetries = 3

[env]
PORT = "3000"
OPENCLAW_API_URL = "http://localhost:14444"

startCommand = "cd backend && uvicorn main:socket_app --host 0.0.0.0 --port $PORT"
buildCommand = "cd backend && pip install -r requirements.txt"
